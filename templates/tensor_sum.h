void tensor_sum_@FIELD_NAME@(@FIELD_CTYPE@ const * alpha,
                             tensor_h const A,
                             char const * idx_A,
                             @FIELD_CTYPE@ const * beta,
                             tensor_h const B,
                             char const * idx_B)

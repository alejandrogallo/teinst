void tensor_contract_@FIELD_NAME@(@FIELD_CTYPE@ const * alpha,
                                  tensor_h const A,
                                  char const * idx_A,
                                  tensor_h const B,
                                  char const * idx_B,
                                  @FIELD_CTYPE@ const * beta,
                                  char const * idx_C,
                                  tensor_h C)
